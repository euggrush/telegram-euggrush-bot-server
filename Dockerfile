# 1. Используем Node.js в качестве базового образа
FROM node:18-alpine

# 2. Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# 3. Копируем package.json и package-lock.json
COPY package*.json ./

# 4. Устанавливаем зависимости
RUN npm install

# 5. Копируем весь код проекта внутрь контейнера
COPY . .

# 6. Открываем порт (если используем вебхук)
EXPOSE 3000

# 7. Команда для запуска бота
CMD ["node", "bot.js"]
